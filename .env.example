# Copy this file to .env and fill in your store details.

# Storefront API token generated through the BigCommerce control panel.
# Required if you want to query product data via the Storefront API.
BIGCOMMERCE_STOREFRONT_API_TOKEN=

# Your BigCommerce store hash (found in the API token details). Do not
# include the leading 'store-' prefix when using in API requests.
BIGCOMMERCE_STORE_HASH=

# Category ID from which products will be offered as add-ons. If set,
# the server will automatically query products in this category and
# present them as add-on options. Leave blank to rely solely on
# manual mappings via POST /api/settings.
ADD_ON_CATEGORY_ID=

# Port to run the Express server on.
PORT=3000
